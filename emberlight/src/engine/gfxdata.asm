; ============================================================================
;  gfxdata.asm  --  pulls in everything tools/build_assets.py generates
;
;  manifest.inc lists the font, the sprites, every picture and the story
;  script, so adding a scene never means touching the assembly.
; ============================================================================

INCLUDE "manifest.inc"
